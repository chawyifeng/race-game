SET FOREIGN_KEY_CHECKS = 0;

-- Drop the 'events' and 'events_days' tables
DROP TABLE IF EXISTS `events`, `event_days`, `customers`, `admins`;

-- Re-enable foreign key checks
SET FOREIGN_KEY_CHECKS = 1;




---------------
SHOW TABLES;
